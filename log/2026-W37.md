# 第 37 周（2026-09-07 ~ 09-13）

> ⚠️ 修订说明：初稿把 Week 2/3/4 写成「一口气补完 / 顺延三周」，**是错的**。
> 实际情况：CS50P 于 2026-09-06（周日）**中午**开课，到 09-07 17:22 为止**只学了两天**，
> 25 题全部是两天内新做的，不存在积压、也不存在拖延。d2l 更不是「顺延三周」——
> 它 09-05 才被写进计划，两天而已，还没开始，谈不上顺延。

## 真实时间线

| 时间 | 事件 |
|---|---|
| 09-05 | 定学习计划，建 `D:\llm-journey` 仓库，CUDA 版 torch 装好 |
| 09-06 中午 | **CS50P 开课**（决定用它替代读书作为 Python 基础阶段教材） |
| 09-06 21:43 | 提交 Week 0 + Week 1，10 题（`f80f503`） |
| 09-07 17:22 | 提交 Week 2 + Week 3 + Week 4，15 题（`29699ca`） |
| **合计** | **25 题 / 不到 49 小时**（这两天基本是废寝忘食的状态） |

## 本周目标

- [x] CS50P Week 2（Loops）5 题
- [x] CS50P Week 3（Exceptions）4 题
- [x] CS50P Week 4（Libraries）6 题
- [ ] CS50P Week 5（Unit Tests）
- [ ] d2l 第 2 章 2.1–2.7（两天前才排进计划，尚未开始，不是顺延）

## 每日记录

### 周日（09-06）· 开课第一天
- 中午开课，当天做完 Week 0（5 题）+ Week 1（5 题）
- 晚 21:43 打包 `cs50p_week0.zip` / `cs50p_week1.zip` 传回电脑，完成首次归档

### 周一（09-07）· 开课第二天
- 做完 Week 2（5 题）+ Week 3（4 题）+ Week 4（6 题）
- 17:19 打包三个 zip 传回，17:22 归档提交
- 环境：补装 `emoji` `pyfiglet` `inflect` —— Week 4 依赖，此前本机没有，题目会直接 ImportError

## 验证结果（本机全量跑，44 个用例）

手机上写完没法执行，所以每批作业归档时我在这台机器上实跑一遍。
测试脚本：`.venv\Scripts\python.exe tests\cs50p_smoke.py`（覆盖 W0–W4 全部 25 题）

**通过 41 / 失败 3 / 跳过 2**

- 跳过原因：`bitcoin.py` 需要 CoinCap v3 的 API key；`figlet.py` 随机字体的输出只能人眼看。

### 三个真 bug（对照官方规格确认）

| 文件 | 问题 | 官方规格原文 |
|---|---|---|
| `week2/plates.py` | 只检查了**第一个**字符是字母，`A2` 会被判成 Valid | “All vanity plates must start with **at least two letters**.” |
| `week3/taqueria.py` | 只在 EOF 时打印一次总价，没有每加一项就打印累计 | “**After each inputted item**, display the total cost of all items inputted thus far” |
| `week3/taqueria.py` | 菜单里 Baja Taco 是 `4.00`，官方 2023-10-25 起改为 `4.25` | 价格表已更新，实测应为 `Total: $12.25` |

另有两处**不影响判分**的小隐患，记录在 `cs50p/README.md` 的「已知问题」一节。

## 质量观察

- Week 4 的 `adieu.py` 用了 `inflect.join()`，把「1 人 / 2 人 / 3 人」三种输出格式一次性处理干净，比手写 if 拼字符串稳。
- 输入结束用 `try / except EOFError`，是 CS50P 的标准写法。
- `plates.py` 手搓了合法字符集列表而不是用 `str.isalnum()`——写得很直白，但漏了「开头两个字母」这条，说明**规则读漏了一条**，不是不会写。

## 周复盘 5 问

1. **本阶段完成了什么？**
   - Week 2–4 共 15 题，累计 CS50P 25 题，两天完成。

2. **哪一件事卡得最久？为什么？**
   - 手机上写完没法真正运行，Week 4 依赖的三个库本机又没有——这类问题在手机上完全看不出来。
   - 对策已固化：每批作业归档时跑 `tests/cs50p_smoke.py`，缺的库顺手装上。

3. **下周最重要的三件事？**
   - CS50P Week 5（Unit Tests）
   - 修掉上面三个 bug（都是几分钟的量，关键是规格要看全）
   - 抽半天开 d2l 第 2 章

4. **有什么可以砍掉？**
   - 暂时不碰论文、不碰书，CS50P 这条线正顺，别打断节奏。

5. **本阶段有可展示的产出吗？**
   - 有：25 个作业已进 GitHub，提交历史连续，贡献绿点正常。

## 下周目标

- [ ] CS50P Week 5：Unit Tests（jar + test_jar）
- [ ] 修 `plates.py` / `taqueria.py` 三个 bug
- [ ] d2l 第 2 章 2.1–2.7
- [ ] 保持每批作业归档时同步跑全量测试
